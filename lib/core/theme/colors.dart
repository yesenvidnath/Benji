import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF007AFF);    // iOS Blue
  static const Color primaryLight = Color(0xFF47A0FF);
  static const Color primaryDark = Color(0xFF0056B3);

  // Accent Colors
  static const Color accent = Color(0xFF5856D6);     // Soft Purple

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF8E8E93);
  static const Color textHint = Color(0xFFC7C7CC);

  // Input Fields
  static const Color inputBackground = Color(0xFFF2F2F7);
  static const Color inputBorder = Color(0xFFD1D1D6);

  // Button Colors
  static const Color buttonPrimary = Color(0xFF007AFF);
  static const Color buttonText = Colors.white;

  // Background Colors
  static const Color background = Color(0xFFF2F2F7);
  static const Color surface = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF34C759);   // iOS Green
  static const Color error = Color(0xFFFF3B30);     // iOS Red
  static const Color warning = Color(0xFFFF9500);   // iOS Orange

    // Glass effect overlay colors
  static const Color glassBackground = Color(0x80FFFFFF); // White with opacity for glass effect
  static const Color glassBorder = Color(0x33FFFFFF); // Subtle white border for glass effect

  // Profile screen colors
  static const Color profilePrimaryText = Color(0xFF333333); // Darker text color for contrast
  static const Color profileSecondaryText = Color(0xFF6D6D6D); // Lighter secondary text color
  static const Color profileAccent = Color(0xFF007AFF); // Blue accent for links and highlights

  // Button colors
  static const Color primaryButton = Color(0xFF007AFF); // iOS-style blue button color
  static const Color errorText = Color(0xFFFF3B30); // iOS-style error red

  static const Color spendingRed = Color(0xFFFF3B30);  // iOS Red for Total Spending
  static const Color savingsGreen = Color(0xFF34C759); // iOS Green for Total Savings
  
}